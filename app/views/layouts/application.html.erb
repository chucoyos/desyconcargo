<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "DesyConCargo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="DesyConCargo">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render "shared/toast" %>
    
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200 fixed top-0 left-0 right-0 z-40">
      <div class="container mx-auto px-5">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">            <%= link_to root_path, class: "text-xl font-bold text-gray-900" do %>
              <span class="text-green-600">Desy</span>ConCargo
            <% end %>
          </div>
          
          <div class="hidden md:flex space-x-8">            <%= link_to "Inicio", root_path, class: "text-gray-700 hover:text-green-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200" %>
            <%= link_to "Líneas Navieras", shipping_lines_path, class: "text-gray-700 hover:text-green-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200" %>
            <%= link_to "Buques", vessels_path, class: "text-gray-700 hover:text-green-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200" %>
            <%= link_to "Puertos", ports_path, class: "text-gray-700 hover:text-green-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200" %>
          </div>
          
          <!-- Mobile menu button -->
          <div class="md:hidden">
            <button type="button" class="text-gray-700 hover:text-green-600 focus:outline-none focus:text-green-600" id="mobile-menu-button">
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
              </svg>
            </button>
          </div>
        </div>
        <div class="md:hidden hidden" id="mobile-menu">
          <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-t border-gray-200">
            <%= link_to "Inicio", root_path, class: "text-gray-700 hover:text-green-600 block px-3 py-2 rounded-md text-base font-medium" %>
            <%= link_to "Líneas Navieras", shipping_lines_path, class: "text-gray-700 hover:text-green-600 block px-3 py-2 rounded-md text-base font-medium" %>
            <%= link_to "Buques", vessels_path, class: "text-gray-700 hover:text-green-600 block px-3 py-2 rounded-md text-base font-medium" %>
            <%= link_to "Puertos", ports_path, class: "text-gray-700 hover:text-green-600 block px-3 py-2 rounded-md text-base font-medium" %>
          </div>
        </div>
      </div>
    </nav>
    
    <main class="pt-16">
      <%= yield %>
    </main>
  </body>
</html>
